// import { useToast } from "@/hooks/use-toast"



// export default function ShowToast({type, title, description}) {
//     const { toast } = useToast()
//   toast({
//     title,
//     description,
//     variant: type === "success" ? "success" : "destructive", // Use "success" or "error"
//   });
// }
